删除所有 code walk 相关代码，仅限三个文件，不影响 PTY/终端等其它功能：

**1. 删除整个文件** `src/main/walkthrough-routes.js`（codewalk 的 HTTP 路由模块，整文件都是 codewalk）

**2. `launcher.js` 删 2 处**：
- 删除 walkthroughRoutesHandler 的 require 注册块（约 L125）
- 删除 HTTP 调度调用 `if (walkthroughRoutesHandler(req, res, urlPath)) return;`（约 L231）

**3. `src/react/TerminalMode.jsx` 删 7 处**（按行号从大到小执行）：
- L344-377：JSX 评论输入 overlay 整块删（保留外层容器和 tabs.map）
- L314-328：handleCommentSubmit/Cancel + focus effect 整块删
- L231-232：`window.cloeCodeWalk` 赋值删
- L205-227：onData 里的 codewalk 键盘分支删，**保留 L204 控制字符防护 和 L228 `window.electronAPI.ptyWrite(tabId, data)`**
- L140-199：`entry._codeWalk = {...}` 对象整块删
- L106+L108：删 `if (!entry._codeWalk?.active)` 防护的两行，**保留 L107 `entry.xterm.write(data)`**（变无条件写入）
- L50-53：删 showInput/inputRef/codeWalkRef 声明，**保留 L54 xtermReady**

安全保证（已逐项验证）：PTY 输入输出/初始化/resize/tab管理/主题全部保留；删除的 state 变量无悬空 JSX 引用。

验证：node --check 全文件 + 部署手测终端 PTY 输入输出正常。